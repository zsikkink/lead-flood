{
  "name": "@lead-onslaught/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash ./scripts/dev.sh",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src test --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm test:unit && pnpm test:integration",
    "test:unit": "vitest run src",
    "test:integration": "vitest run test/integration"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@lead-onslaught/contracts": "workspace:*",
    "@lead-onslaught/db": "workspace:*",
    "@lead-onslaught/observability": "workspace:*",
    "fastify": "^5.2.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
