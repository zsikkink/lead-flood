{
  "name": "@lead-flood/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash ./scripts/dev.sh",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm test:unit",
    "test:unit": "vitest run src"
  },
  "dependencies": {
    "@lead-flood/db": "workspace:*",
    "@lead-flood/observability": "workspace:*",
    "pg-boss": "^10.3.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
